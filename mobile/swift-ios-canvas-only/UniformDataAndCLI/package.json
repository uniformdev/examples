{
  "scripts": {
    "pull:content": "uniform sync pull",
    "push:content": "uniform sync push",
    "pull:styles": "design-extensions-tools pull",
    "push:styles": "design-extensions-tools push",
    "uniform:pull": "run-s pull:content pull:styles",
    "uniform:push": "run-s push:content push:styles"
  },
  "devDependencies": {
    "@uniformdev/cli": "^20.42.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@uniformdev/design-extensions-tools": "^6.0.104"
  }
}
